import queue
import numpy as np

# TODO generate games using agent
def selfplay(agent, queue):

    np.random.dirichlet(alpha=0.03, size=(legal_moves.shape))

    z = v
    
    queue.append((state, policy, z))

