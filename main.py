import queue
import time

from evaluator import evaluator
from selfplay import selfplay

def run_pipeline():
    time_limit = 60
    start = time.time()
    # TODO initialize two agents, the best and the competitor

    # TODO initialize queue of game history
    queue = queue.Queue()

    # TODO go ham and make these three components
    while (time.time() - start) < time_limit:
        pass



    return best_agent






